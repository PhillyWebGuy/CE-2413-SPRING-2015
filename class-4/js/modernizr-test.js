$(function() {
  
  console.dir(Modernizr);
  
  //Exercise:
  //1) Remove the CSS in the sample HTML that displays/hides the divs
  //2) Write JS that checks to see if Modernizr.canvas == true
  //3) If Modernizer.canvas == true, then show the canvas div by using/hide "upgrad
  //browser" div
  //JQuery and JQuery's CSS method
  //4) If Modernizer.canvas == false, then hide canvas div, show "upgrade browser"
  //div
  
    
});